# PostgreSQL Client Authentication Configuration File
# TYPE  DATABASE        USER            ADDRESS                 METHOD

# Local connections
local   all             all                                     trust

# IPv4 local connections
host    all             all             127.0.0.1/32            scram-sha-256

# IPv6 local connections
host    all             all             ::1/128                 scram-sha-256

# Allow all connections from Docker internal network (waddlebot services)
host    all             all             172.16.0.0/12           scram-sha-256
host    all             all             192.168.0.0/16          scram-sha-256
host    all             all             10.0.0.0/8              scram-sha-256

# Replication connections - allow from any Docker internal network host
host    replication     waddlebot       172.16.0.0/12           scram-sha-256
host    replication     waddlebot       192.168.0.0/16          scram-sha-256
host    replication     waddlebot       10.0.0.0/8              scram-sha-256
host    replication     all             0.0.0.0/0               scram-sha-256

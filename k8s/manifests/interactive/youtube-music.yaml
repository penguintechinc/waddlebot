apiVersion: apps/v1
kind: Deployment
metadata:
  name: youtube-music-interaction
  namespace: waddlebot
  labels:
    app: youtube-music-interaction
    component: interactive
spec:
  replicas: 1
  selector:
    matchLabels:
      app: youtube-music-interaction
  template:
    metadata:
      labels:
        app: youtube-music-interaction
        component: interactive
    spec:
      containers:
      - name: youtube-music-interaction
        image: localhost:32000/waddlebot/youtube-music-interaction:latest
        imagePullPolicy: Always
        ports:
        - containerPort: 8025
          name: http
        envFrom:
        - configMapRef:
            name: waddlebot-config
        - secretRef:
            name: waddlebot-secrets
        livenessProbe:
          httpGet:
            path: /health
            port: 8025
          initialDelaySeconds: 30
          periodSeconds: 10
          timeoutSeconds: 5
          failureThreshold: 3
        readinessProbe:
          httpGet:
            path: /health
            port: 8025
          initialDelaySeconds: 10
          periodSeconds: 5
          timeoutSeconds: 3
          failureThreshold: 3
        resources:
          requests:
            memory: "256Mi"
            cpu: "100m"
          limits:
            memory: "512Mi"
            cpu: "500m"
---
apiVersion: v1
kind: Service
metadata:
  name: youtube-music-interaction
  namespace: waddlebot
  labels:
    app: youtube-music-interaction
    component: interactive
spec:
  type: ClusterIP
  ports:
  - port: 8025
    targetPort: 8025
    protocol: TCP
    name: http
  selector:
    app: youtube-music-interaction

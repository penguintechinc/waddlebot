apiVersion: apps/v1
kind: Deployment
metadata:
  name: calendar-interaction
  namespace: waddlebot
  labels:
    app: calendar-interaction
    component: interactive
spec:
  replicas: 1
  selector:
    matchLabels:
      app: calendar-interaction
  template:
    metadata:
      labels:
        app: calendar-interaction
        component: interactive
    spec:
      containers:
      - name: calendar-interaction
        image: localhost:32000/waddlebot/calendar-interaction:latest
        imagePullPolicy: Always
        ports:
        - containerPort: 8030
          name: http
        envFrom:
        - configMapRef:
            name: waddlebot-config
        - secretRef:
            name: waddlebot-secrets
        livenessProbe:
          httpGet:
            path: /health
            port: 8030
          initialDelaySeconds: 30
          periodSeconds: 10
          timeoutSeconds: 5
          failureThreshold: 3
        readinessProbe:
          httpGet:
            path: /health
            port: 8030
          initialDelaySeconds: 10
          periodSeconds: 5
          timeoutSeconds: 3
          failureThreshold: 3
        resources:
          requests:
            memory: "256Mi"
            cpu: "100m"
          limits:
            memory: "512Mi"
            cpu: "500m"
---
apiVersion: v1
kind: Service
metadata:
  name: calendar-interaction
  namespace: waddlebot
  labels:
    app: calendar-interaction
    component: interactive
spec:
  type: ClusterIP
  ports:
  - port: 8030
    targetPort: 8030
    protocol: TCP
    name: http
  selector:
    app: calendar-interaction

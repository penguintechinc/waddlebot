apiVersion: apps/v1
kind: Deployment
metadata:
  name: memories-interaction
  namespace: waddlebot
  labels:
    app: memories-interaction
    component: interactive
spec:
  replicas: 1
  selector:
    matchLabels:
      app: memories-interaction
  template:
    metadata:
      labels:
        app: memories-interaction
        component: interactive
    spec:
      containers:
      - name: memories-interaction
        image: localhost:32000/waddlebot/memories-interaction:latest
        imagePullPolicy: Always
        ports:
        - containerPort: 8031
          name: http
        envFrom:
        - configMapRef:
            name: waddlebot-config
        - secretRef:
            name: waddlebot-secrets
        livenessProbe:
          httpGet:
            path: /health
            port: 8031
          initialDelaySeconds: 30
          periodSeconds: 10
          timeoutSeconds: 5
          failureThreshold: 3
        readinessProbe:
          httpGet:
            path: /health
            port: 8031
          initialDelaySeconds: 10
          periodSeconds: 5
          timeoutSeconds: 3
          failureThreshold: 3
        resources:
          requests:
            memory: "256Mi"
            cpu: "100m"
          limits:
            memory: "512Mi"
            cpu: "500m"
---
apiVersion: v1
kind: Service
metadata:
  name: memories-interaction
  namespace: waddlebot
  labels:
    app: memories-interaction
    component: interactive
spec:
  type: ClusterIP
  ports:
  - port: 8031
    targetPort: 8031
    protocol: TCP
    name: http
  selector:
    app: memories-interaction

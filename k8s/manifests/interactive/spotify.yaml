apiVersion: apps/v1
kind: Deployment
metadata:
  name: spotify-interaction
  namespace: waddlebot
  labels:
    app: spotify-interaction
    component: interactive
spec:
  replicas: 1
  selector:
    matchLabels:
      app: spotify-interaction
  template:
    metadata:
      labels:
        app: spotify-interaction
        component: interactive
    spec:
      containers:
      - name: spotify-interaction
        image: localhost:32000/waddlebot/spotify-interaction:latest
        imagePullPolicy: Always
        ports:
        - containerPort: 8026
          name: http
        envFrom:
        - configMapRef:
            name: waddlebot-config
        - secretRef:
            name: waddlebot-secrets
        livenessProbe:
          httpGet:
            path: /health
            port: 8026
          initialDelaySeconds: 30
          periodSeconds: 10
          timeoutSeconds: 5
          failureThreshold: 3
        readinessProbe:
          httpGet:
            path: /health
            port: 8026
          initialDelaySeconds: 10
          periodSeconds: 5
          timeoutSeconds: 3
          failureThreshold: 3
        resources:
          requests:
            memory: "256Mi"
            cpu: "100m"
          limits:
            memory: "512Mi"
            cpu: "500m"
---
apiVersion: v1
kind: Service
metadata:
  name: spotify-interaction
  namespace: waddlebot
  labels:
    app: spotify-interaction
    component: interactive
spec:
  type: ClusterIP
  ports:
  - port: 8026
    targetPort: 8026
    protocol: TCP
    name: http
  selector:
    app: spotify-interaction

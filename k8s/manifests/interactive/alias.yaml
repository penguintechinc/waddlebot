apiVersion: apps/v1
kind: Deployment
metadata:
  name: alias-interaction
  namespace: waddlebot
  labels:
    app: alias-interaction
    component: interactive
spec:
  replicas: 1
  selector:
    matchLabels:
      app: alias-interaction
  template:
    metadata:
      labels:
        app: alias-interaction
        component: interactive
    spec:
      containers:
      - name: alias-interaction
        image: localhost:32000/waddlebot/alias-interaction:latest
        imagePullPolicy: Always
        ports:
        - containerPort: 8010
          name: http
        envFrom:
        - configMapRef:
            name: waddlebot-config
        - secretRef:
            name: waddlebot-secrets
        livenessProbe:
          httpGet:
            path: /health
            port: 8010
          initialDelaySeconds: 30
          periodSeconds: 10
          timeoutSeconds: 5
          failureThreshold: 3
        readinessProbe:
          httpGet:
            path: /health
            port: 8010
          initialDelaySeconds: 10
          periodSeconds: 5
          timeoutSeconds: 3
          failureThreshold: 3
        resources:
          requests:
            memory: "256Mi"
            cpu: "100m"
          limits:
            memory: "512Mi"
            cpu: "500m"
---
apiVersion: v1
kind: Service
metadata:
  name: alias-interaction
  namespace: waddlebot
  labels:
    app: alias-interaction
    component: interactive
spec:
  type: ClusterIP
  ports:
  - port: 8010
    targetPort: 8010
    protocol: TCP
    name: http
  selector:
    app: alias-interaction

apiVersion: apps/v1
kind: Deployment
metadata:
  name: inventory-interaction
  namespace: waddlebot
  labels:
    app: inventory-interaction
    component: interactive
spec:
  replicas: 1
  selector:
    matchLabels:
      app: inventory-interaction
  template:
    metadata:
      labels:
        app: inventory-interaction
        component: interactive
    spec:
      containers:
      - name: inventory-interaction
        image: localhost:32000/waddlebot/inventory-interaction:latest
        imagePullPolicy: Always
        ports:
        - containerPort: 8024
          name: http
        envFrom:
        - configMapRef:
            name: waddlebot-config
        - secretRef:
            name: waddlebot-secrets
        livenessProbe:
          httpGet:
            path: /health
            port: 8024
          initialDelaySeconds: 30
          periodSeconds: 10
          timeoutSeconds: 5
          failureThreshold: 3
        readinessProbe:
          httpGet:
            path: /health
            port: 8024
          initialDelaySeconds: 10
          periodSeconds: 5
          timeoutSeconds: 3
          failureThreshold: 3
        resources:
          requests:
            memory: "256Mi"
            cpu: "100m"
          limits:
            memory: "512Mi"
            cpu: "500m"
---
apiVersion: v1
kind: Service
metadata:
  name: inventory-interaction
  namespace: waddlebot
  labels:
    app: inventory-interaction
    component: interactive
spec:
  type: ClusterIP
  ports:
  - port: 8024
    targetPort: 8024
    protocol: TCP
    name: http
  selector:
    app: inventory-interaction

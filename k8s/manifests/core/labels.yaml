apiVersion: apps/v1
kind: Deployment
metadata:
  name: labels-core
  namespace: waddlebot
  labels:
    app: labels-core
    component: core
spec:
  replicas: 1
  selector:
    matchLabels:
      app: labels-core
  template:
    metadata:
      labels:
        app: labels-core
        component: core
    spec:
      containers:
      - name: labels-core
        image: localhost:32000/waddlebot/labels-core:latest
        imagePullPolicy: Always
        ports:
        - containerPort: 8023
          name: http
        envFrom:
        - configMapRef:
            name: waddlebot-config
        - secretRef:
            name: waddlebot-secrets
        livenessProbe:
          httpGet:
            path: /health
            port: 8023
          initialDelaySeconds: 30
          periodSeconds: 10
          timeoutSeconds: 5
          failureThreshold: 3
        readinessProbe:
          httpGet:
            path: /health
            port: 8023
          initialDelaySeconds: 10
          periodSeconds: 5
          timeoutSeconds: 3
          failureThreshold: 3
        resources:
          requests:
            memory: "256Mi"
            cpu: "100m"
          limits:
            memory: "512Mi"
            cpu: "500m"
---
apiVersion: v1
kind: Service
metadata:
  name: labels-core
  namespace: waddlebot
  labels:
    app: labels-core
    component: core
spec:
  type: ClusterIP
  ports:
  - port: 8023
    targetPort: 8023
    protocol: TCP
    name: http
  selector:
    app: labels-core

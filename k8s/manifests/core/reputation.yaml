apiVersion: apps/v1
kind: Deployment
metadata:
  name: reputation
  namespace: waddlebot
  labels:
    app: reputation
    component: core
spec:
  replicas: 1
  selector:
    matchLabels:
      app: reputation
  template:
    metadata:
      labels:
        app: reputation
        component: core
    spec:
      containers:
      - name: reputation
        image: localhost:32000/waddlebot/reputation:latest
        imagePullPolicy: Always
        ports:
        - containerPort: 8021
          name: http
        envFrom:
        - configMapRef:
            name: waddlebot-config
        - secretRef:
            name: waddlebot-secrets
        livenessProbe:
          httpGet:
            path: /health
            port: 8021
          initialDelaySeconds: 30
          periodSeconds: 10
          timeoutSeconds: 5
          failureThreshold: 3
        readinessProbe:
          httpGet:
            path: /health
            port: 8021
          initialDelaySeconds: 10
          periodSeconds: 5
          timeoutSeconds: 3
          failureThreshold: 3
        resources:
          requests:
            memory: "256Mi"
            cpu: "100m"
          limits:
            memory: "512Mi"
            cpu: "500m"
---
apiVersion: v1
kind: Service
metadata:
  name: reputation
  namespace: waddlebot
  labels:
    app: reputation
    component: core
spec:
  type: ClusterIP
  ports:
  - port: 8021
    targetPort: 8021
    protocol: TCP
    name: http
  selector:
    app: reputation

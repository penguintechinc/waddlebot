apiVersion: apps/v1
kind: Deployment
metadata:
  name: youtube-live-collector
  namespace: waddlebot
  labels:
    app: youtube-live-collector
    component: collector
spec:
  replicas: 1
  selector:
    matchLabels:
      app: youtube-live-collector
  template:
    metadata:
      labels:
        app: youtube-live-collector
        component: collector
    spec:
      containers:
      - name: youtube-live-collector
        image: localhost:32000/waddlebot/youtube-live-collector:latest
        imagePullPolicy: Always
        ports:
        - containerPort: 8006
          name: http
        envFrom:
        - configMapRef:
            name: waddlebot-config
        - secretRef:
            name: waddlebot-secrets
        livenessProbe:
          httpGet:
            path: /health
            port: 8006
          initialDelaySeconds: 30
          periodSeconds: 10
          timeoutSeconds: 5
          failureThreshold: 3
        readinessProbe:
          httpGet:
            path: /health
            port: 8006
          initialDelaySeconds: 10
          periodSeconds: 5
          timeoutSeconds: 3
          failureThreshold: 3
        resources:
          requests:
            memory: "256Mi"
            cpu: "100m"
          limits:
            memory: "512Mi"
            cpu: "500m"
---
apiVersion: v1
kind: Service
metadata:
  name: youtube-live-collector
  namespace: waddlebot
  labels:
    app: youtube-live-collector
    component: collector
spec:
  type: ClusterIP
  ports:
  - port: 8006
    targetPort: 8006
    protocol: TCP
    name: http
  selector:
    app: youtube-live-collector

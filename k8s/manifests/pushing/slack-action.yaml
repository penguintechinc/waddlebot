apiVersion: apps/v1
kind: Deployment
metadata:
  name: slack-action
  labels:
    app.kubernetes.io/name: slack-action
    app.kubernetes.io/component: pushing
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: slack-action
  template:
    metadata:
      labels:
        app.kubernetes.io/name: slack-action
        app.kubernetes.io/component: pushing
    spec:
      containers:
        - name: slack-action
          image: localhost:32000/waddlebot/slack-action:latest
          imagePullPolicy: Always
          ports:
            - name: rest
              containerPort: 8071
            - name: grpc
              containerPort: 50052
          envFrom:
            - configMapRef:
                name: waddlebot-config
            - secretRef:
                name: waddlebot-secrets
          env:
            - name: MODULE_NAME
              value: "slack-action"
            - name: REST_PORT
              value: "8071"
            - name: GRPC_PORT
              value: "50052"
          livenessProbe:
            httpGet:
              path: /health
              port: 8071
            initialDelaySeconds: 30
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /health
              port: 8071
            initialDelaySeconds: 10
            periodSeconds: 5
          resources:
            requests:
              memory: "256Mi"
              cpu: "100m"
            limits:
              memory: "512Mi"
              cpu: "500m"
---
apiVersion: v1
kind: Service
metadata:
  name: slack-action
  labels:
    app.kubernetes.io/name: slack-action
    app.kubernetes.io/component: pushing
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: slack-action
  ports:
    - name: rest
      port: 8071
      targetPort: 8071
    - name: grpc
      port: 50052
      targetPort: 50052

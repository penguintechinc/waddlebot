apiVersion: apps/v1
kind: Deployment
metadata:
  name: youtube-action
  labels:
    app.kubernetes.io/name: youtube-action
    app.kubernetes.io/component: pushing
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: youtube-action
  template:
    metadata:
      labels:
        app.kubernetes.io/name: youtube-action
        app.kubernetes.io/component: pushing
    spec:
      containers:
        - name: youtube-action
          image: localhost:32000/waddlebot/youtube-action:latest
          imagePullPolicy: Always
          ports:
            - name: rest
              containerPort: 8073
            - name: grpc
              containerPort: 50054
          envFrom:
            - configMapRef:
                name: waddlebot-config
            - secretRef:
                name: waddlebot-secrets
          env:
            - name: MODULE_NAME
              value: "youtube-action"
            - name: REST_PORT
              value: "8073"
            - name: GRPC_PORT
              value: "50054"
          livenessProbe:
            httpGet:
              path: /health
              port: 8073
            initialDelaySeconds: 30
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /health
              port: 8073
            initialDelaySeconds: 10
            periodSeconds: 5
          resources:
            requests:
              memory: "256Mi"
              cpu: "100m"
            limits:
              memory: "512Mi"
              cpu: "500m"
---
apiVersion: v1
kind: Service
metadata:
  name: youtube-action
  labels:
    app.kubernetes.io/name: youtube-action
    app.kubernetes.io/component: pushing
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: youtube-action
  ports:
    - name: rest
      port: 8073
      targetPort: 8073
    - name: grpc
      port: 50054
      targetPort: 50054

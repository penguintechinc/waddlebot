# WaddleBot Flask Module - Dependencies

# Core Framework
quart>=0.19.0
hypercorn>=0.16.0

# HTTP Client (async)
httpx>=0.27.0,<0.28.0
aiohttp>=3.10.0,<4.0.0

# Environment
python-dotenv>=1.0.0

# Language Detection (Ensemble approach - 3 detectors for 95%+ accuracy)
# Note: Using Python 3.12 for fasttext C++ compatibility
# Pin NumPy <2.0 for fasttext-wheel compatibility (np.array copy= keyword changed)
numpy<2.0
fasttext-wheel>=0.9.2
lingua-language-detector==2.1.1
langdetect==1.0.9

# Translation & Natural Language Processing
deep-translator>=1.11.4  # Modern replacement for googletrans (compatible with httpx)
cachetools>=5.3.2
pydantic>=2.10.0  # Python 3.13 compatible with pre-built wheels

# Development
pytest>=7.4.0
pytest-asyncio>=0.23.0
pytest-cov>=4.1.0

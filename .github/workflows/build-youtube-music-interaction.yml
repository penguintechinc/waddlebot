name: Build youtube-music-interaction

on:
  push:
    branches: [ main, develop, 'feature/**', 'bugfix/**' ]
    paths:
      - 'action/interactive/youtube_music_interaction_module/**'
      - 'libs/flask_core/**'
      - '.version'
      - '.github/workflows/build-youtube-music-interaction.yml'
      - '.github/workflows/build-container.yml'
  pull_request:
    branches: [ main, develop ]
    paths:
      - 'action/interactive/youtube_music_interaction_module/**'
      - 'libs/flask_core/**'
  workflow_dispatch:

jobs:
  build:
    name: Build youtube-music-interaction Container
    uses: ./.github/workflows/build-container.yml
    with:
      module_path: 'action/interactive/youtube_music_interaction_module'
      module_name: 'youtube-music-interaction'
      module_port: '8015'
    secrets: inherit
